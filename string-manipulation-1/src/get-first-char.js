/* exported getFirstChar */
function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}

// must return first character of string
// strings are array like so i can just treat as such
// first char of string ~~~ first element in array
// get string at 0th index and assign to variable
// return variable
