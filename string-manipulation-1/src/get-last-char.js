/* exported getLastChar */
function getLastChar(string) {
  var lastChar = string[string.length - 1];
  return lastChar;
}

// need to return last char of string
// string is array like
// to get last char must get last index
// last index is equal to string length minus 1 bc indexes start at 0
// assign that to a variable
// return variable
