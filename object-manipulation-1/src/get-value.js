/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// - have to access object
// - then have to access its value
// - what method can i use to get this?
// - object[key]
// return it ^^
