/* exported last */
function last(array) {
  var lastElement = array[array.length - 1];
  return lastElement;
}

// must return last element of array
// how can i access last element
// get array length
// subtract 1 from it then use that as index to access
// assign that to variable
// return variable
