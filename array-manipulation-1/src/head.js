/* exported head */
function head(array) {
  var firstElement = array[0];
  return firstElement;
}

// must return first element of array
// how can i access first element of array?
// array at index 0 would get the first element
// assign array at index 0 to a variable
// return that variable
